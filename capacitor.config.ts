import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.temubarang.pro2026',
  appName: 'TemuBarang Pro 2026',
  webDir: 'www'
};

export default config;
